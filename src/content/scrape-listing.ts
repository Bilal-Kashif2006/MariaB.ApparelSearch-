// Injected on a Bareeze category/listing page (e.g. /casuals, /formals).
// Reads real product cards straight off the rendered DOM — title, price,
// image, and detail-page link — confirmed selectors in README.md.
export {};
